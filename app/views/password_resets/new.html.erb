<div align="Center">
  <h1>Recuperacion de contraseña</h1>
</div>
    <%= form_for(@usuario, :url => url_for(:controller => 'password_resets', :action => 'create'), :html => {class: "form-horizontal", role: "form"}) do |f| %>
      <div class='form-group'>
        <div class='control-label col-sm-2'>
        <%= f.label :email %>:
        </div>
        <div class='col-sm-8'>
          <%= f.text_field :email, class: "form-control", placeholder: "Ingrese su correo", autofocus: true %>
        </div>
      </div>
    
      <div class="center form-group">
        <div class="col-sm-offset-2 col-md-8">
          <%= f.submit "Enviar correo" ,class: 'btn btn-primary btn-lg' %>
        </div>
        <% debug :email %>
      </div>
      <% end %>
