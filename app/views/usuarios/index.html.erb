<h1 align="center">Usuarios de Alpha Blog</h1>
<div align="center">
  <%= will_paginate %>
  <% @usuarios.each do |usuario| %>
    <ul class="listing">
      <div class="row">
        <div class="well col-md-4 col-md-offset-4">
          <li> <%= link_to gravatar_for(usuario), usuario_path(usuario) %> </li>
          <li class"articulo-titulo">
            <%= link_to usuario.usuario, usuario_path(usuario) %>
          </li>
          <li><small><%= pluralize(usuario.articulos.count, "articulo") if usuario.articulos %></small></li>
          <% if logged_in? and current_user.admin? %>
          <li><%= link_to "Eliminar este usuario", usuario_path(usuario), method: :delete,
              data: { confirm: "¿Estas seguro que quieres eliminar este usuario y todos sus articulos?" } %></li>
          <% end %>
        </div>
      </div>
    </ul>
  <% end %>
  <%= will_paginate %>
</div>